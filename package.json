{
  "name": "pitchproof",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Compile a prospect's own content and brand into a self-contained, offline, branchable proof artifact.",
  "scripts": {
    "build": "node scripts/build.mjs",
    "test": "node --test --test-concurrency=4 \"test/**/*.test.mjs\"",
    "verify:offline": "node scripts/verify-offline.mjs",
    "verify:determinism": "node scripts/lint-determinism.mjs",
    "verify": "node scripts/lint-determinism.mjs && INTEGRATION_STRICT=1 node --test \"test/**/*.test.mjs\" && node scripts/build.mjs --verify-repeat && node scripts/verify-offline.mjs",
    "verify:conformance": "INTEGRATION_STRICT=1 node --test test/integration/api-conformance.test.mjs"
  },
  "devDependencies": {
    "playwright": "^1.56.1"
  },
  "engines": {
    "node": ">=22"
  }
}
